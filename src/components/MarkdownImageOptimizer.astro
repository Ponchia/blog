---
import OptimizedImage from './OptimizedImage.astro';
---

<div class="image-optimization-guide">
  <h3>Beyond Hero Images: Optimize Content Images Too</h3>
  
  <div class="comparison">
    <div class="before">
      <h4>Before: Standard Markdown Image</h4>
      <pre><code>![Alt text](/blog-placeholder-4.jpg)</code></pre>
      <img src="/blog-placeholder-4.jpg" alt="Standard Markdown format" class="sample-image" />
      <p class="note">Standard format, larger file size.</p>
    </div>
    
    <div class="after">
      <h4>After: Using OptimizedImage Component</h4>
      <pre><code>&lt;OptimizedImage 
  src="/blog-placeholder-4.webp" 
  alt="Optimized format" 
  width={600}
  height={400}
  format="webp"
/&gt;</code></pre>
      <OptimizedImage 
        src="/blog-placeholder-4.webp" 
        alt="Optimized format" 
        width={600}
        height={400}
        format="webp"
        class="sample-image"
      />
      <p class="note">WebP format, ~30% smaller file size.</p>
    </div>
  </div>
  
  <div class="tips">
    <h4>Quick Tips:</h4>
    <ul>
      <li>Import the component at the top of your MDX file: <code>import OptimizedImage from '../../components/OptimizedImage.astro';</code></li>
      <li>Replace standard markdown images with the component for better performance</li>
      <li>Always specify width and height to prevent layout shifts</li>
      <li>Use modern formats (WebP, AVIF) for better compression</li>
    </ul>
  </div>
</div>

<style>
  .image-optimization-guide {
    margin: 2rem 0;
    padding: 1.5rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    background-color: var(--bg-secondary);
  }
  
  h3 {
    margin-top: 0;
    font-size: 1.5rem;
  }
  
  h4 {
    margin: 1rem 0 0.5rem;
    font-size: 1.2rem;
  }
  
  .comparison {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin: 1.5rem 0;
  }
  
  .sample-image {
    width: 100%;
    height: auto;
    border-radius: 4px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }
  
  pre {
    background-color: var(--code-bg);
    padding: 0.75rem;
    border-radius: 4px;
    overflow-x: auto;
    font-size: 0.85rem;
    margin: 0.75rem 0;
  }
  
  code {
    font-family: 'JetBrains Mono', monospace;
  }
  
  .note {
    font-size: 0.85rem;
    color: var(--accent-light);
    margin-top: 0.5rem;
  }
  
  .tips {
    margin-top: 1.5rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border-color);
  }
  
  .tips ul {
    margin: 0.5rem 0 0;
    padding-left: 1.5rem;
  }
  
  .tips li {
    margin-bottom: 0.5rem;
  }
  
  @media (max-width: 768px) {
    .comparison {
      grid-template-columns: 1fr;
    }
  }
</style> 